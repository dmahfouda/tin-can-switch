version: "3.3"
services:
  node:
    image: node:13
    user: node
    ports:
    - "8080:8080"
    networks:
    - backend
    volumes:
    - type: bind
      source: /home/mikey/tin-can/tin-can-switch/src
      target: /app
      read_only: true
    depends_on:
    - mongo
    command: ["/bin/sh", "-c", "cd /app && npm start"]
  mongo:
    image: mongo:3.4.24
    networks:
    - backend
networks:
  backend:    
